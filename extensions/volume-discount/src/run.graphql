query RunInput {
  cart {
    lines {
      quantity
      merchandise {
        __typename
        ...on ProductVariant {
            id
        }
      }
    }
  }
  discountNode {
     metafield(namespace: "$app:function-configuration", key: "volume_discount"){
      value
    }
  }
}
